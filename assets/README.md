# Plugin assets

This directory is reserved for future plugin assets (icons, theme tokens, prompt snippets). The current build of `kimi-memory` ships empty here; the directory is kept (with `assets/.gitkeep`) so the manifest and test suite can rely on a stable relative path. See `kimi.plugin.json` for the active tool/hook surface.
